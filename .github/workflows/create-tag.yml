name: Create Tag
on:
  workflow_dispatch:

jobs:
  create_tag:
    steps:
      - uses: go-semantic-release/action@v1
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          changelog-file: true
